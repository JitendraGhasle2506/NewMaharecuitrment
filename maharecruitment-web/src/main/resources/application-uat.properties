#spring.datasource.url=jdbc:postgresql://10.0.1.193:5432/mahaitrecritment1
# spring.datasource.url=jdbc:postgresql://10.10.85.21:5432/maharecruitment
# spring.datasource.username=postgres
# spring.datasource.password=P@stgres#HAt987

spring.datasource.url=jdbc:postgresql://10.10.85.21:5432/maharecruitment
#spring.datasource.url=jdbc:postgresql://localhost:5432/maharecruitmentpostgres

spring.datasource.username=postgres
spring.datasource.password=P@stgres#HAt987


# spring.datasource.password=root
spring.jpa.show-sql=false
logging.level.root=INFO
logging.level.com.maharecruitment.gov.in=DEBUG
logging.level.com.project=DEBUG
logging.level.org.springframework.transaction=INFO
logging.level.com.fasterxml.jackson=INFO
logging.file.path=${MAHARECRUITMENT_LOG_PATH:${user.home}/maharecruitment/logs/uat}
app.security.cookie.secure=${APP_SECURITY_COOKIE_SECURE:true}
app.security.cookie.same-site=Lax
server.forward-headers-strategy=${SERVER_FORWARD_HEADERS_STRATEGY:framework}
app.security.transport.require-https=${APP_SECURITY_REQUIRE_HTTPS:true}
app.security.transport.allow-loopback-http=false
app.security.transport.trust-forwarded-headers=${APP_SECURITY_TRUST_FORWARDED_HEADERS:true}
app.base-url=${APP_BASE_URL:https://mahahrseva.mahait.org}
server.servlet.context-path=${SERVER_SERVLET_CONTEXT_PATH:/}
security.host-validation.enabled=${SECURITY_HOST_VALIDATION_ENABLED:false}
server.servlet.session.cookie.secure=${app.security.cookie.secure}
server.servlet.session.cookie.same-site=${app.security.cookie.same-site}

# Store uploaded files in the UAT server root data directory.
#file.upload.base-path=/data/uploaded-files
#department.manpower.work-order.base-path=/data/uploaded-files
#department.profile.documents.base-path=/data/uploaded-files
#department.payment.receipt.base-path=/data/uploaded-files
#workorder.document.base-path=/data/uploaded-files

file.upload.base-path=${user.home}/uploaded-files
department.manpower.work-order.base-path=${user.home}/uploaded-files/work-orders
department.profile.documents.base-path=${user.home}/uploaded-files/profiles
department.payment.receipt.base-path=${user.home}/uploaded-files/receipts
workorder.document.base-path=${user.home}/uploaded-files/workorders

registration.department.otp-bypass-enabled=false
app.service.sms-enabled=${SMS_ENABLED:false}
app.service.email-enabled=${EMAIL_ENABLED:true}

# Temporarily disable account lockout after repeated failed logins in UAT.
security.login-protection.enabled=false

# The UAT attendance endpoint presents a certificate chain unavailable to the JVM trust store.
attendance.integration.internal.trust-all-certificates=true

